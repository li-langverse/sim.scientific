# Release notes: 2026-05-25 — readme-wp-a3

**Status:** Ready for review  
**Repo:** li-langverse/sim.scientific  
**PR:** #4  
**PH / REQ:** PH-SCI (org hygiene WP-A3)  
**Author:** agent (WP-A3)

---

## Summary (one sentence)

Adds a public `README.md` with purpose, link to `sim`, and `lic check` smoke so the mirror repo passes org hygiene (empty README audit).

## Agent continuation (required)

1. Read: `README.md`, `src/lib.li`, `li.toml`; [`lic` sim-output-contract](https://github.com/li-langverse/lic/blob/main/docs/ecosystem/sim-output-contract.md) for dispatch surface.
2. Run: `gh pr checks` on this PR; after merge, `gh repo view li-langverse/sim.scientific` shows README on GitHub.
3. Then: continue PH-SCI algorithm work in `lic` / benchmarks ingest — not blocked on this docs PR.
4. Blocked on: **none** for README; optional human: refine GitHub description if `gh repo edit` lacked admin rights (see PR body).

## Changed (specific)

| Area | What | Evidence |
|------|------|----------|
| Docs | `README.md` — PH-SCI purpose, `sim` link, `lic check src/lib.li` smoke | File added; matches `.github/workflows/ci.yml` check step |
| Docs | `CHANGELOG.md` `[Unreleased]` | Keep a Changelog entry |
| Docs | `docs/release-notes/2026-05-25-readme-wp-a3.md` | Agent-oriented release note |

## Not changed (scope fence)

- `src/lib.li` simulation smokes and `run_algo` registry — **not** modified.
- `li.toml` path deps and `github_repo` metadata (`li-sim-scientific` URLs) — **not** renamed in this PR.
- [`sim`](https://github.com/li-langverse/sim) core API — **not** touched.
- `lic` CI image / `actions/cache` platform work (WP-C*) — **not** in scope.

## Breaking changes

None.

## Security

N/A — documentation-only; no trusted axioms or CVE surface.

## Performance

N/A — no benchmark or harness changes.

## Downstream

| Repo | Action |
|------|--------|
| lip / lit / lic monorepo | N/A — mirror README only; monorepo package path unchanged |

## CHANGELOG entry (paste into Unreleased)

```markdown
### Added
- `README.md` — purpose, link to `sim`, `lic check` build smoke (WP-A3, PH-SCI).
```
